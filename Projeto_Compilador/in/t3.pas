program TestSumLiterals;
begin
  // No código máquina deve apenas aparecer  "PUSHS "Hello, world!""
  writeln('Hello, ' + 'world' + ' !');

  // No código máquina deve apenas existir o push do resultado da soma
  writeln(10 + 25 + 0);
end.